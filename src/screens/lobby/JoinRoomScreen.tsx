import { Panel } from "../../components/cards/Panel";

export function JoinRoomScreen() {
  return (
    <Panel title="Join Room">
      <p>Join flow placeholder. The route is ready for a room code form in the next milestone.</p>
    </Panel>
  );
}
